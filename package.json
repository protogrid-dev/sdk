{
  "name": "protogrid-sdks",
  "private": true,
  "version": "0.0.0",
  "description": "protogrid SDKs: TypeScript (@protogrid/sdk) and Python (protogrid-sdk).",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.13.5",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
